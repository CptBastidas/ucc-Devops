	    cout << "\n";
            int argc
            char const *argv[]
        {
            double numero= vf_area;
            double RaizCuadrada = sqrt(vf_area);
            cout << "la raiz cuadrada es => " << numero << RaizCuadrada << ;
	    cout << "La area es => " << vf_area << endl;

	// archivo funcion
	//  bf_ area calcular la rais cuadrada de una variable e imprimir el  resultado

		return 0;
	}
}
